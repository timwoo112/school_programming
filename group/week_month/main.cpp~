#include <iostream>

using namespace std;

int main()
{
  // Declare variables
  float total;
  float input;
  string selector_string;

  // Ask user if they want to input weekly or monthly sales
  cout << "Do you want to input daily or monthly sales: Enter weekly or monthly ";
  cin >> selector_string;

  // Iterate on the users selection allowing them to enter data
  if (selector_string

  return 0;
}
